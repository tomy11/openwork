apiVersion: v1
kind: Secret
metadata:
  name: openwork-mysql
  labels:
    app.kubernetes.io/name: openwork-mysql
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: password
  MYSQL_DATABASE: openwork_den
  MYSQL_USER: openwork
  MYSQL_PASSWORD: openwork
---
apiVersion: v1
kind: Service
metadata:
  name: openwork-mysql
  labels:
    app.kubernetes.io/name: openwork-mysql
spec:
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app.kubernetes.io/name: openwork-mysql
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openwork-mysql
  labels:
    app.kubernetes.io/name: openwork-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openwork-mysql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: openwork-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          imagePullPolicy: IfNotPresent
          ports:
            - name: mysql
              containerPort: 3306
          envFrom:
            - secretRef:
                name: openwork-mysql
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
          readinessProbe:
            exec:
              command:
                - sh
                - -lc
                - mysqladmin ping -h 127.0.0.1 -uopenwork -p"$MYSQL_PASSWORD"
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24
      volumes:
        - name: data
          emptyDir: {}
