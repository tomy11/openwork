apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: openwork-den-allow-core-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: openwork-ee
      app.kubernetes.io/instance: openwork-ee
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - den-api
          - den-web
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: openwork-mysql
      ports:
        - protocol: TCP
          port: 3306
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: openwork-ee
              app.kubernetes.io/instance: openwork-ee
            matchExpressions:
              - key: app.kubernetes.io/component
                operator: In
                values:
                  - den-api
                  - den-web
