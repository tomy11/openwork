apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: openwork-den-default-deny-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: openwork-ee
      app.kubernetes.io/instance: openwork-ee
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - den-api
          - den-web
  policyTypes:
    - Egress
  egress: []
